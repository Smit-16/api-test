<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_product_group_tree" model="ir.ui.view">
        <field name="name">product.group.tree</field>
        <field name="model">product.group</field>
        <field name="arch" type="xml">
            <tree string="Product Groups">
                <field name="name"/>
                <field name="code"/>
                <field name="parent_id"/>
                <field name="product_count"/>
                <field name="active" widget="boolean_toggle"/>
            </tree>
        </field>
    </record>

    <record id="view_product_group_form" model="ir.ui.view">
        <field name="name">product.group.form</field>
        <field name="model">product.group</field>
        <field name="arch" type="xml">
            <form string="Product Group">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(product.product_template_action)d" type="action" class="oe_stat_button" icon="fa-cubes" context="{'search_default_product_group_id': active_id}">
                            <field name="product_count" widget="statinfo" string="Products"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <group>
                        <group>
                            <field name="name" placeholder="e.g. Electronics"/>
                            <field name="code" placeholder="e.g. ELEC"/>
                            <field name="parent_id"/>
                        </group>
                        <group>
                            <field name="active" invisible="1"/>
                            <field name="color" widget="color_picker"/>
                        </group>
                    </group>
                    <group>
                        <field name="description" placeholder="Description of the product group..."/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_product_group_search" model="ir.ui.view">
        <field name="name">product.group.search</field>
        <field name="model">product.group</field>
        <field name="arch" type="xml">
            <search string="Product Groups">
                <field name="name" string="Group" filter_domain="['|', ('name', 'ilike', self), ('code', 'ilike', self)]"/>
                <field name="parent_id"/>
                <filter string="Active" name="active" domain="[('active', '=', True)]"/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                <separator/>
                <filter string="Top Level" name="top_level" domain="[('parent_id', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Parent Group" name="group_by_parent" context="{'group_by': 'parent_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_product_group" model="ir.actions.act_window">
        <field name="name">Product Groups</field>
        <field name="res_model">product.group</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a Product Group
            </p>
            <p>
                Product groups help you categorize and organize your products for better management and reporting.
            </p>
        </field>
    </record>
</odoo>
